# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual Environment
.venv/
venv/
ven/
.venv-pdf/
env/
ENV/

# Flask
flask_session/
instance/
.webassets-cache

# Static build output (regenerated by freeze.py / Cloudflare build)
dist/

# Wrangler / Cloudflare local cache
.wrangler/
node_modules/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Personal files — never commit resumes or local exports (keep them out of the repo)
*.pdf
*.docx
**/resume.pdf
**/Resume.pdf

# One deliberate exception: the resume published at /resume. This is the only
# personal document meant to be public, so it is tracked on purpose. Nothing
# else matching the rules above should ever be un-ignored.
!static/William_Nayar_Resume.pdf
